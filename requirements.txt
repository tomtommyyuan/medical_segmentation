numpy>=1.24
scipy>=1.11
opencv-python>=4.8
scikit-image>=0.21
matplotlib>=3.7
torch>=2.0
torchvision>=0.15

# Foundation-model encoders. timm loads UNI, transformers loads Phikon-v2.
# Neither is needed for --encoder conv, which is the offline fallback and the
# "no foundation model" ablation.
timm>=1.0
# Capped below 5: HFViTEncoder passes interpolate_pos_encoding=True so a
# 224-pretrained ViT accepts 256px patches, and transformers 5 reworked that
# call. The test suite runs without transformers installed by design, so it
# cannot catch the break - only a real encoder run does.
transformers>=4.40,<5

pytest>=7.4
